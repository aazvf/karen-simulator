{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "nuxt generate && aws s3 sync .output/public s3://karen.aazvf.art/ --profile s3deploy --delete"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.12",
    "axios": "^0.27.2",
    "bootstrap": "^5.2.1",
    "nuxt": "3.0.0-rc.11",
    "postcss": "^8.4.16",
    "sass": "^1.55.0",
    "sass-loader": "^13.0.2"
  }
}
